extdata: EXTDATA
history: HIST
root: GCM

mapl:
  setServices:
    sharedObj: libMAPL.cap
  children:

    GCM:
      config_file: GCM.yaml

    EXTDATA:
      config_file: extdata.yaml

    HIST:
      config_file: history.yaml

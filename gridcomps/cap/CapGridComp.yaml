extdata: EXTDATA
history: HIST
root: GCM

mapl:
  children:

    GCM:
      dso: libGEOS.GcmGridComp
      config_file: GCM.yaml

    EXTDATA:
      dso: libMAPL.ExtData
      config_file: extdata.yaml

    HIST:
      dso: libMAPL.history
      config_file: history.yaml

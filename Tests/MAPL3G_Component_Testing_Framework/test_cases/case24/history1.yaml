run_next_step: false

active_collections:
  - test

time_specs:
  three_hour: &one_hour
    frequency: PT1H

collections:
  test:
    template: "%c.nc4"
    time_spec: *one_hour
    var_list:
      E_1: {source: E_1}
      #E_2: {source: E_2}

persist_closest with wide valid_range and partial on-disk data. Only 2005 monthly files
exist on disk; valid_range covers 2004-2006. refine_valid_range narrows to the actual
on-disk files and intersects with the run period so that the run (20050615) succeeds
without requiring files outside the run window to be present.

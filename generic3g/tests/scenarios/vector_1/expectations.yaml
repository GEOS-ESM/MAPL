# For each component:
#   - provide a path to the outer/user componen in the hierarchy
#   - list the fields expected in each import/export/internal states
#   - annotate whether field is "complete"

- component: child_A/<user>
  export:
    B(u,v): {status: complete, class: bundle}
- component: child_A
  export:
    B(u,v): {status: complete, class: bundle}

- component: child_B/<user>
  import:
    B(u,v): {status: complete, class: bundle}
- component: child_B
  import:
    B(u,v): {status: complete, class: bundle}
- component: <user>
  import: {}
  export: {}
  internal: {}
- component: <root>
  export:
    child_A/B(u,v): {status: complete, class: bundle}

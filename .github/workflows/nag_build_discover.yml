name: NAG Build (Discover)

on:
  workflow_dispatch:
  pull_request:
    paths-ignore:
      - "**.md"
      - "**.json"
      - "Python/**"
      - ".github/CODEOWNERS"
      - ".github/PULL_REQUEST_TEMPLATE.md"
      - ".editorconfig"

defaults:
  run:
    shell: bash

jobs:
  run-nag-build-discover:
    uses: GEOS-ESM/CI-workflows/.github/workflows/nag-build.yml@project/mapl

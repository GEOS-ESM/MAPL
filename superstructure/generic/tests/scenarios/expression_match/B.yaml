mapl:
  states:
    import:
      # No standard_name/long_name declared - matches how a History var_list
      # entry (`E_sum: {source: E_sum}`) references an export by name without
      # redeclaring its metadata.
      E_sum:
        units: m
        vertical_dim_spec: NONE

    export: {}

    internal: {}

  setServices:
    sharedObj: libconfigurable_gridcomp

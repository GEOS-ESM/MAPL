mapl:
  states:
    import:
      # I1 standard_name changed to match its connected Export (child_A's
      # E1) - previously declared its own independent placeholder, which
      # disagrees under STRICT ValidationMode.
      I1:
        standard_name: 'E1'
        units: 'm'
        vertical_dim_spec: NONE

  setServices:
    sharedObj: libconfigurable_gridcomp
